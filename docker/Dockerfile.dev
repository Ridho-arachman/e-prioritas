FROM node:25-alpine

WORKDIR /app

# Install runtime deps
RUN apk add --no-cache \
  libc6-compat \
  openssl \
  libssl3 \
  libstdc++ \
  python3 \
  make \
  g++

RUN npm install -g pnpm

COPY package.json pnpm-lock.yaml ./
RUN pnpm install

COPY . .

EXPOSE 3000

CMD ["pnpm", "dev"]